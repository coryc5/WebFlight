const chai = require('chai')
const expect = chai.expect
const assert = chai.assert

const addStatusBar = require('../../lib/addStatusBar')

describe('addStatusBar', () => {
  it('files widget should be appended to the page', () => {

  })
  it('widget should have dropdown functionality', () => {

  })
  it('dropdown should contain correct number of files', () => {

  })
  it('close button should work', () => {

  })
})
