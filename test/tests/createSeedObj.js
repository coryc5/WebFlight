/* global describe, it */

'use strict'

const chai = require('chai')
const assert = chai.assert

const createSeedObj = '../lib/createSeedObj'

describe('createSeedObj', () => {
  it('should return an object', () => {
    assert.equal(true, true)
  })
})
